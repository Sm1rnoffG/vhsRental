CREATE TABLE movie (
    id INTEGER PRIMARY KEY NOT NULL,
    name TEXT NOT NULL,
    length INTEGER NOT NULL,
    available_copies INTEGER NOT NULL,
    currently_available INTEGER NOT NULL,
    format INTEGER NOT NULL,
    age_rating INTEGER NOT NULL,
    image_url TEXT NOT NULL,
    imdb_url TEXT NOT NULL,
    release_year INTEGER NOT NULL,
    description TEXT NOT NULL,
    genre INTEGER NOT NULL
);

selectAll:
SELECT *
FROM movie
ORDER BY name;

deleteMovie:
DELETE FROM movie
WHERE id = ?;


addMovie:
INSERT OR REPLACE INTO movie (
    id,
    name,
    length,
    available_copies,
    currently_available,
    format,
    age_rating,
    image_url,
    imdb_url,
    release_year,
    description,
    genre
)
VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?);

INSERT OR REPLACE INTO movie(
      id,
      name,
      length,
      available_copies,
      currently_available,
      format,
      age_rating,
      image_url,
      imdb_url,
      release_year,
      description,
      genre
)
VALUES (0, "Up", 96, 1, 1, 0, 3, "https://lumiere-a.akamaihd.net/v1/images/p_up_19753_e6f911e3.jpeg", "https://www.imdb.com/title/tt1049413/", 2009, "film", 2),
(1, "Up", 96, 1, 1, 2, 3, "", "https://www.imdb.com/title/tt1049413/", 2009, "film", 2);
